spring:
  application:
    name: green-zuul
  cloud:
    config:
      lable: master
      profile: dev
 #     url: http://localhost:9999/
      discovery:
        enabled: true #是否从配置中心读取        
        serviceId: green-config #配置中心服务名